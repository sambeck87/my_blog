  <%= bootstrap_form_with model: @comment, url: user_post_path(User.find(params[:user_id])), method: :post, local: true do |f|%>
    <%= f.text_area :text %>
    <%= f.submit 'Send comment' %>
  <%end%>
